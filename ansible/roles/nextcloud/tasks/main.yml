---
# tasks file for nextcloud

- name: Create the nextcloud group
  group:
    name: nextcloud
    system: true
    state: present

- name: Create the nextcloud user
  user:
    name: nextcloud
    group: nextcloud
    system: yes
    createhome: no
    state: present
